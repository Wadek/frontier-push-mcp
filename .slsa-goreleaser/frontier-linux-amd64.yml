version: 1
env:
  - GO111MODULE=on
  - CGO_ENABLED=0
flags:
  - -trimpath
  - -buildvcs=false
goos: linux
goarch: amd64
main: ./cmd/frontier
dir: .
binary: frontier-linux-amd64
ldflags:
  - "-s -w"
  - "-X main.version={{ .Env.VERSION }}"
  - "-X main.commit={{ .Env.COMMIT }}"

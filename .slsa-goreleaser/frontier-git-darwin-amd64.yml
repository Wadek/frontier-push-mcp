version: 1
env:
  - GO111MODULE=on
  - CGO_ENABLED=0
flags:
  - -trimpath
  - -buildvcs=false
goos: darwin
goarch: amd64
main: ./cmd/frontier-git
dir: .
binary: frontier-git-darwin-amd64
ldflags:
  - "-s -w"
  - "-X main.version={{ .Env.VERSION }}"
  - "-X main.commit={{ .Env.COMMIT }}"
